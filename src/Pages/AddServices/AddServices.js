import React from 'react';
import { useTitle } from '../TitleHooks/useTitle';

const AddServices = () => {
    useTitle('Add-Services-Page')
    return (
        <div>
            addd services
        </div>
    );
};

export default AddServices;